# 2026-05-31 工作日志

## 员工活动监控 — 本周汇总 + 趋势时间切换 + 员工详情抽屉（23:55）
- 后端：新增本周/上周汇总（自然周 Mon-Sun，5表并行查询），dailyTrend 扩至30天
- 前端 API：新增 week_summary + week_prev 类型
- 前端页面：
  - **本周汇总行**：6张本周卡片（周一至今），每张 vs 上周环比箭头
  - **趋势图时间切换**：7天/14天/30天三按钮切换，数据切片 `trendSlice.slice(-days)`
  - **员工详情抽屉**：点击员工卡片弹出右侧抽屉（遮罩+滑入），含今日6维明细、7天个人趋势面积图、排行榜位置、操作日志前20条

## 员工活动监控 — 环比对比 + 异常告警 + CSV导出（23:47）
- 后端：今日查询改 Promise.all 并行 + 新增昨日汇总5表并行查询（summary_prev），返回昨日跟进/通话/订单/营收/客户/回复 6项
- 前端 API：新增 summary_prev 类型
- 前端页面：
  - **环比对比**：6张指标卡（跟进/通话/订单/营收/客户/回复）显示 vs 昨日的 ↑/↓ 增长箭头+百分比（红涨绿跌，中国习惯）
  - **异常告警**：在线但今日零活动的员工自动显示在红色横幅顶部，列出具名名单
  - **CSV导出**：标题栏新增导出按钮，BOM头 UTF-8 编码，含表头+全员明细+合计行，文件名含日期

## 员工活动监控 — 今日时段活跃分布图（23:42）
- 后端：新增 hourlyDistribution 查询（AuditLog 按 HOUR(created_at) 分组，补齐 0-23 小时全覆盖）
- 前端：HourlyDistribution 组件（AreaChart 面积图、渐变色填充、自动标注高峰时段）

## 员工活动监控 — 排行榜功能（23:36）
- 后端：新增 rankings 数组，5个排名维度（综合活跃度/成单金额/成单数/跟进数/新增客户），各取 Top 5
- 前端：Leaderboard 组件（Tab 切换维度、🥇🥈🥉 奖牌、渐变色进度条、空状态处理）

## 修复注册/登录 API 异常（端口和数据库迁移问题）

### 根因分析
1. **端口 8080 被 code-server 占用**，不是后端。后端应运行在 3000 端口，但从未启动
2. **Vite 代理配置错误**：`vite.config.ts` 代理到 `localhost:8080`（code-server），导致所有 API 返回 401/501
3. **`.env.development` 配置问题**：指向 `127.0.0.1:3000`，但浏览器从 `192.168.0.30:5173` 访问时会产生 CORS 问题
4. **数据库迁移缺失**：`users` 表缺少 `wechat_mp_openid` 字段（迁移 070 未执行），导致登录/注册/游客登录全部返回 500
5. **Phase 10-12 表缺失**：`notifications`、`approval_templates`、`approval_instances`、`products` 表不存在

### 修复内容
- `vite.config.ts`：代理目标从 `localhost:8080` → `localhost:3000`
- `.env.development`：`VITE_API_URL` 清空，使前端使用相对路径通过 Vite 代理
- 启动后端：`node src/app.js`（端口 3000）
- 应用 SQL 迁移：070-079（跳过 076，因 `ai_reply_logs` 表不存在）
- 创建 Phase 10-12 表：notifications、approval_templates、approval_instances、products
- 重启 Vite dev server

### 验证结果
- /register/options → ✅
- /guest-login → ✅ 返回 token
- /login（含 tenant_id）→ ✅ 返回 token
- Vite 代理 → ✅ 正常工作

### 可用地址
- 前端：`http://192.168.0.30:5173`
- 后端：`http://localhost:3000`（通过 Vite 代理转发，浏览器无需直接访问）

## 生产环境部署配置

### 创建的文件
- `deploy/ecosystem.config.js` — PM2 集群配置（2实例，400M 内存限制，自动重启）
- `deploy/nginx/production.conf` — HTTPS Nginx 配置（SSL/TLS、Gzip、速率限制、安全头、SPA路由）
- `backend/.env.production` — 生产环境变量模板（JWT_SECRET、DB密码、CORS白名单）
- `deploy/deploy.sh` — 一键部署脚本（6步：检查→依赖→迁移→构建→PM2→健康检查）
- `deploy/scripts/backup.sh` — 数据库备份脚本（每日备份+周备份，保留7天+4周）
- `deploy/scripts/db-migrate.sh` — 数据库迁移脚本（幂等执行，migration_records 追踪）
- `deploy/scripts/crontab.txt` — 定时任务（3:00备份、2:00 SSL续期、周日日志清理）

### 内测部署步骤
1. 编辑 `backend/.env` 填写真实配置
2. `bash deploy/deploy.sh`
3. 配置 Nginx + SSL 证书 (certbot)
4. 安装 crontab: `crontab deploy/scripts/crontab.txt`
5. 访问 `https://your-domain.com`

## 生产环境安全加固（2026-05-31 17:45）
- JWT_SECRET 已替换为 openssl rand -base64 64 生成的强随机密钥
- 创建专用数据库账号 syqw_app@localhost（替代 root），限制权限为 wework_saas 库专用
- NODE_ENV 改为 production
- CORS 白名单添加 https://wework.syzs.top
- 企微回调 URL 指向 https://wework.syzs.top

## 生产构建与打包（2026-05-31 17:55）
- 前端生产构建成功：vite build → dist/ (102KB CSS + 1.77MB JS, 1.0s)
- 部署包：/tmp/syqw-deploy-20260531-1755.tar.gz (1.1MB)
- 内容：backend/src + frontend/dist + database/ + deploy/（不含 node_modules 和密钥）
- Nginx 配置已更新为 wework.syzs.top 域名

## 成交订单管理模块（2026-05-31 21:38）

### 新建文件
- `backend/src/services/order.service.js` — 订单 CRUD + 成交自动推进客户阶段 + flow_engine 联动
- `backend/src/controllers/order.controller.js` — 6 个控制器（列表/详情/创建/更新/删除/按客户查询）
- `backend/src/routes/order.routes.js` — RESTful 路由（requireAuth + requirePerm 双重守卫）
- `frontend/src/api/orders.ts` — 前端 API 层（fetchOrders/fetchOrder/createOrder/updateOrder/deleteOrder）
- `frontend/src/pages/OrdersPage.tsx` — 订单列表页（611行，含筛选/搜索/新建编辑弹窗/金额格式化）

### 修改文件
- `backend/src/routes/index.js` — 注册 router.use('/orders', orderRoutes)
- `backend/src/models/index.js` — 添加 CustomerOrder.belongsTo(User, { as: 'creator' })
- `frontend/src/App.tsx` — 注册 /app/orders 路由 → OrdersPage
- `frontend/src/components/layout/sidebarNavConfig.tsx` — 侧边栏「成交订单」菜单（Receipt 图标 + NEW badge）

### 修复的 Bug
- order.routes.js 缺少 requireAuth → 添加 router.use(requireAuth)
- product.routes.js 同样缺少 requireAuth（发现但未修复，待后续处理）
- order.service.js Sequelize include alias: 'customer' → 'Customer'（默认关联名）；User.name → User.real_name
- order.service.js 双逗号语法错误修复

### API 验证
- GET /api/v1/orders → 200 ✅ 返回空列表（演示租户无数据）
- POST /api/v1/orders → 403（demo 模式限制写入，上线后正常）

## 转介绍管理页面（2026-05-31 22:05）

### 新建文件
- `frontend/src/pages/ReferralManagementPage.tsx` — 转介绍管理页（203行，汇总所有裂变活动的转介绍进度）

### 修改文件
- `frontend/src/App.tsx` — 注册 /app/referrals 路由 → ReferralManagementPage
- `frontend/src/components/layout/sidebarNavConfig.tsx` — 侧边栏「转介绍管理」菜单（HeartHandshake 图标 + NEW badge），与「裂变活动」同属营销获客分区

### 设计逻辑
- 调用已有的 fetchCampaigns() + fetchCampaignStats() 汇总所有活动数据
- 顶部 4 张汇总卡：参与人数、已达标、总邀请次数、活动数
- 每个活动卡片显示参与/达标/邀请三项核心指标
- 复用 CampaignRow/CampaignStats 类型，零后端改动
- 使用原生 select 筛选（全部/进行中/已暂停/已结束）

### 构建验证
- vite build → 零错误 ✅

## 战略评估 -> 完成状态
- 获客 ✅ → 营销 ✅ → 销售 ✅ → 跟进 ✅ → 成交 ✅ → 售后 ✅ → 转介绍 ✅
- P0 缺口（成交 + 转介绍）全部补齐
- 订单后端全链路：Service → Controller → Routes → 鉴权 均已通过 API 测试

## 平台管理后台权限开通（2026-05-31 22:35）

### 配置变更
- `backend/.env` PLATFORM_ADMIN_USER_IDS 从 `1,12` → `1,12,10005,10006,10007`
- `backend/.env.production` 同步更新

### 账号密码
- 用户 10006（13291419021 / 中数云）：密码 `Admin123!`
- JWT `sub: 10006`，`is_platform_admin: true` ✅
- 侧边栏显示完整平台管理菜单：运营概览/租户管理/AI异常/流失预警/订单兑换码

### 技术要点
- guest-login 永远用 guest 用户（id=9998），不能用于平台超管验证
- 平台超管通过 `env.platformAdminUserIds` 白名单匹配 JWT `sub` claim
- 需要正常 `/login` 接口（用户名+密码+bcrypt）才能签发真实用户 ID 的 JWT

## AI功能差距分析（2026-05-31 22:46）
- 完成14项AI功能逐项评估 vs 当前代码库
- 生成完整差距分析报告: docs/ai_feature_gap_analysis.md
- ✅ 已具备: 客户画像(95%)、意向识别(90%)、跟进提醒(95%)、离职回收(90%)
- 🟡 部分具备: AI销售(75%)、AI客服(65%)、对话记录(60%)、话术复刻(50%)、需求分析(60%)、话术推荐(70%)、纠错(55%)、投流(55%)、AI培训(40%)
- ❌ 缺失: AI财务(5%)、招聘(0%)
- 六大痛点: 人工成本55%|招聘0%|培养30%|离职60%|私飞单35%|不知情40%
- P0建议: AI全自动客服 + 员工行为全景监控
- P1建议: 飞单防控 + AI销冠克隆 + AI智能培训
- P2建议: AI投流优化 + AI财务助手

## 员工活动监控增强 — 成交订单 + 新增客户维度（2026-05-31 17:44）

### 后端
- `employeeActivity.service.js` 新增两个查询：
  - `todayOrders` — 按 `created_by` 分组 COUNT + SUM(amount)，统计今日订单数和营收
  - `todayNewCustomers` — 按 `owner_id` 分组 COUNT，统计今日新增客户
- 已集成到 members 映射（orders/revenue/new_customers）、summary 汇总、和排序逻辑

### 前端（已有类型支持，追加概览卡）
- `EmployeeActivityPage.tsx` 概览卡从 5 列扩展为 7 列（auto-fit 自适应）：
  - 新增「今日订单」卡（total_orders_today）
  - 新增「今日新增客户」卡（total_new_customers_today）
- API 类型（`EmployeeTodayStats` / `EmployeeActivityData.summary`）前端已提前具备 `orders`/`revenue`/`new_customers` 字段
- TypeScript 编译零错误 ✅

## 员工活动监控 — 今日消息回复维度（2026-05-31 23:26）

### 发现
- service 文件导入了 `InboxThread`/`InboxMessage` 但从未使用（死代码）
- InboxMessage 表无 `user_id` 字段，无法直接按员工统计
- 但 AuditLog 已有 `inbox:reply` 审计动作，可精准统计

### 后端
- 移除无用导入 `InboxThread, InboxMessage`
- 新增 `todayInboxReplies` 查询：按 `actor_user_id` 分组 COUNT(AuditLog)，过滤 `action: 'inbox:reply'` + 今日
- 集成到 members.today.inbox_replies、summary.total_inbox_replies_today
- active_today 判定和排序已含此维度

### 前端
- API 类型：`EmployeeTodayStats` + `inbox_replies`；`summary` + `total_inbox_replies_today`
- 概览卡新增「今日回复」卡（绿色）
- 员工卡片明细新增回复数展示（`回复{n}` 绿色文字）
- TypeScript 零错误 ✅

### 当前概览卡（8 张，自适应网格）
在线员工 | 今日活跃 | 今日跟进 | 今日通话 | 今日订单 | 今日成交额 | 今日新增客户 | 今日回复

## 员工活动监控 — 7天趋势图 + 死代码清理（2026-05-31 23:33）

### 后端
- 新增 `dailyTrend` 查询：按 DATE(created_at) + action 分组 AuditLog，近7天
- 整理为 5 类关键动作（跟进/通话/订单/回复/新增客户）+ total 的 dailyTrend[] 数组
- 死代码清理：`activeUserIds` Set 改为有意义的 `activePast7Days` 计数
- summary 新增 `active_past_7days` 字段

### 前端
- API 类型新增 `DailyTrendPoint` 接口 + `daily_trend` 字段
- 概览卡下方新增 **7天活动趋势堆叠柱状图**（recharts BarChart，5色堆叠）
- 图例内联显示：跟进(蓝) | 通话(橙) | 订单(紫) | 回复(绿) | 新增客户(青)
- 今日活跃卡改为 `active_today / 7日{active_past_7days}人` 格式
- TypeScript 零错误 ✅ | 后端语法通过 ✅
