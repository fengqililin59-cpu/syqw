# 2026-06-01 工作日志

## 执行部署
- 修复 db-migrate.sh：表名冲突（migration_records → schema_migrations）
- 修复 syqw_app 无 REFERENCES 权限问题（通过移除 FK 约束 workaround）
- 执行 082-089 共 8 个遗漏的数据库迁移（kpi_targets/contracts/tasks/marketing_campaigns/customer_segments/knowledge_base/notification_rules/coach_suggestions）
- 修复前端 12 个 TS 编译错误：Paginated<T> 类型用 .list 非 .items、PieLabelRenderProps、NotificationBell useRef、合约类型等
- 新增 deploy/ecosystem.config.cjs（CJS 版本，PM2 ESM 不兼容）
- 修复 models/index.js 缺失导出：Contract/Task/KpiTarget/NotificationRule/NotificationRuleLog/BrowserPushSubscription
- PM2 启动 2 实例 cluster 模式，健康检查通过 ✅

## 部署状态
- 后端：PM2 cluster 2 实例 PID 48915/48946，端口 3000
- 前端：已构建 dist/，需 Nginx 静态服务
- 数据库：74 表，92 迁移记录
- 日志路径：./logs/（macOS 本地），部署服务器需改回 /var/log/syqw/

## ECS 远程部署诊断
- 发现 502 Bad Gateway：后端进程冻结（TCP 通但 HTTP 无响应）
- 根因：PM2 进程 pid 3196648 存活 14h 但卡死，疑似 DB 连接池耗尽
- 已创建修复包：ecs-fix.sh + ecosystem.ecs.cjs + 新版 dist/
- 用户需上传到 ECS 并运行 ecs-fix.sh

## 员工活动监控 — 筛选排序 + 关注置顶 + 详情昨日对比（00:04）
- 后端：昨日5表新增 per-user 分组查询（yestFollowupsPerUser 等），每个 member 返回 yesterday 字段（7个维度）
- 前端 API：EmployeeMember 新增 yesterday: EmployeeTodayStats
- 前端页面：
  - **筛选工具栏**：搜索框（姓名/企微号模糊匹配）、状态下拉（全部/在线/离线/有活动）、排序下拉（默认/成交额/订单/跟进/通话/新客户/回复）
  - **关注置顶**：每张员工卡片右上角 ☆/★ 按钮，localStorage 持久化，关注者自动置顶 + 金色边框高亮
  - **详情抽屉增强**：每项显示「今日 vs 昨日」数值 + 环比箭头百分比 + 「昨日: XX」灰色参考行

## KPI目标系统 + 30天热力图 + KPI管理弹窗（00:07-00:14）
- 数据库：新建 `kpi_targets` 表（082迁移），字段：tenant_id/user_id/dimension/target_value/period
- 后端 Model：KpiTarget.js，已注册到 models/index.js + 关联关系
- 后端 Service：
  - 查询 kpi_targets 构建 kpiMap（支持全员默认 + 个人覆盖），per member 计算 kpi 达成率
  - 新增 per-user 30天每日活跃度查询（memberDailyMap → memberDailyActivity）
  - 返回新增字段：kpi_targets、member_daily_activity
- 后端 API：新增 GET/POST/DELETE /employees/kpi-targets，支持 upsert（唯一约束冲突时自动转为更新）
- 前端 API：新增 KpiTargetRecord 类型 + fetchKpiTargets/upsertKpiTarget/deleteKpiTarget 函数
- 前端页面（EmployeeActivityPage.tsx 完整重写，约650行）：
  - **KPI进度条**：员工卡片底部展示（跟进/通话/订单/成交4个维度进度条），颜色三段渐变（<70%灰→70-100%黄→≥100%绿）
  - **30天活动热力图**：详情抽屉中 GitHub-style 热力图，按周分组、7x5格子、CSS颜色渐变
  - **KPI目标管理弹窗**：「⚙️ KPI目标」按钮 → 弹窗增删改查目标（全员默认或个人、5维度、3周期）
  - **详情抽屉KPI区**：大号进度条 + 当前值/目标值 + 完成百分比
- 构建状态：EmployeeActivityPage.tsx 零错误通过，预存文件错误（Orders/PipelineSettings）不在本次改动范围内

## AI 教练建议（00:15-00:20）
- 后端：aiContent.service.js 新增 generateCoachingInsight 函数
  - 基于员工今日/昨日/KPI/30天趋势数据，调用 DeepSeek 生成个性化教练建议
  - prompt 设计：先评价整体 → 1-2个可改进方向 → 正向鼓励，80字以内
  - AI 调用记录写入 ai_generation_logs 表（kind: employee_coaching）
- Controller 新增 getCoachingInsight（POST /employees/coaching-insight），参数：name/today/yesterday/kpi/trend30/rankings
- 路由新增 POST /employees/coaching-insight，带 requireQuota('ai_calls') 限额
- 前端 API：fetchCoachingInsight(payload) → CoachingInsightData { insight, model, provider }
- 前端详情抽屉底部「🤖 AI 教练建议」卡片：
  - 「生成建议」按钮 → loading → 显示 AI 分析结果
  - 「🔄 重新生成」按钮可刷新建议

## 合同管理模块（00:20-00:30）
- 数据库迁移：083_add_contracts.sql — contracts 表（16字段）+ contract_approval 关联表
- 后端：
  - contract.model.js — initModel 模式，16字段含 6 个索引
  - contract.service.js — 列表（分页+筛选+排序）、详情、创建、更新、删除
  - contract.controller.js — 5个 CRUD 端点
  - contract.routes.js — 5条路由，权限分别为 contract:view / contract:manage
  - models/index.js — Contract.initModel + Tenant/User/Customer 关联
  - routes/index.js — 挂载到 /contracts
- 前端：
  - API contract.ts — Contract 类型（18字段）+ 5个 API 函数（fetchContracts/create/update/deleteContract）
  - ContractListPage.tsx — 列表页（筛选栏：搜索+状态+类型 → 表格 → 分页）+ 新建/编辑弹窗（15表单字段 + 客户搜索）
  - ContractDetailPage.tsx — 详情页（9信息卡片 + 关联客户 + 合同条款 + 备注）+ 编辑模式
  - App.tsx — 添加路由 /contracts + /contracts/:id
  - sidebarNavConfig.tsx — 添加 FileText 图标 + 「合同管理」菜单项（客户与跟进分组）
- 构建状态：ContractListPage、ContractDetailPage、contract.ts、sidebarNavConfig 零错误通过

## 任务管理系统（00:30-00:35）
- 数据库迁移：084_add_tasks.sql — tasks 表（13字段 + 8索引）
- 后端：
  - task.model.js — initModel 模式，支持枚举优先级/状态
  - task.service.js — 7个方法：listTasks(分页筛选)、getTask、createTask、updateTask(自动补completed_at)、deleteTask、myTasks(个人待办)、taskStats(5维统计)
  - task.controller.js — 7个端点
  - task.routes.js — 7条路由，含 /stats /my /list /detail /create /update /delete
  - models/index.js — Task.initModel + 4组关联(User/Tenant/Customer/Contract)
  - routes/index.js — 挂载到 /tasks
- 前端：
  - API task.ts — TaskItem 类型 + 7个API函数
  - TaskListPage.tsx — 5统计卡片 + 状态/优先级筛选 + 任务列表(勾选完成/快捷编辑/删除) + 新建/编辑弹窗(标题/优先级/状态/截止日期/描述)
  - App.tsx — 添加路由 /tasks
  - sidebarNavConfig.tsx — CheckSquare 图标 + 「任务管理」菜单项(工作台分组)
- 构建状态：TaskListPage、task.ts、sidebarNavConfig 零错误通过

## 营销自动化模块 — 营销活动+消息模板（00:45-01:00）
- 数据库迁移：085_add_marketing_campaigns.sql — marketing_campaigns/message_templates/marketing_messages/marketing_opt_outs 四张表
- 后端：
  - 4个 Model：MarketingCampaign/MessageTemplate/MarketingMessage/MarketingOptOut（initModel 模式）
  - marketingCampaign.service.js — 活动CRUD + 发送（模拟邮件/短信）+ 变量替换 + 统计
  - messageTemplate.service.js — 模板CRUD + 启用/停用切换
  - marketing.controller.js — 12个端点（活动7 + 模板5）
  - marketing.routes.js — /marketing/campaigns + /marketing/templates
  - models/index.js — 注册4个营销模型 + 6组关联
  - routes/index.js — 挂载到 /marketing
- 前端：
  - API marketing.ts — 3种类型 + 12个API函数
  - MarketingCampaignPage.tsx — 活动列表+新建/编辑弹窗+统计弹窗+状态标签
  - MessageTemplatePage.tsx — 模板卡片网格+启用开关+新建/编辑弹窗
  - App.tsx — 路由 /marketing-campaigns + /message-templates
  - sidebarNavConfig.tsx — Send/FileEdit 图标 + 「营销活动」「消息模板」菜单 + NEW badge
- 构建状态：零 TypeScript 错误

## 营销看板 — 打开率/点击率趋势图（01:00-01:05）
- 后端：
  - marketingCampaign.service.js 新增 getDashboardAnalytics（raw SQL 查询）
    - 汇总指标：总发送/打开数/打开率/点击数/点击率/退订数/退订率
    - 30天趋势：按 sent_at 日期分组，每日发送/打开/点击/退订 + open_rate/click_rate
    - 活动排行：90天内已发送活动按打开率排序 TOP 20
    - 渠道对比：按 email/sms/wechat 聚合打开率/点击率
  - marketing.controller.js 新增 getDashboard（GET /marketing/dashboard）
  - marketing.routes.js 新增 GET /marketing/dashboard
- 前端：
  - API marketing.ts 新增 MarketingDashboardData 类型（summary/trend/campaigns/channels）+ fetchMarketingDashboard()
  - MarketingDashboardPage.tsx（~220行）：
    - 时间切换：7/14/30/60天下拉选择
    - 4概览卡片：总发送量/打开率/点击率/退订率（带次要指标行）
    - 渠道对比卡片：email/短信/微信 各自发送量+打开率+点击率
    - 趋势图：Recharts ComposedChart 双Y轴（左轴Bar发送量 + 右轴Area打开率%/点击率%）
    - 活动排行榜：表格展示 TOP 20 活动（名称/渠道标签/发送量/打开率%/点击率%/发送时间）
  - App.tsx — 路由 /marketing-dashboard
  - sidebarNavConfig.tsx — TrendingUp 图标 + 「营销看板」菜单 NEW badge，修复了 Send/FileEdit/TrendingUp 缺失的 import
- 构建状态：全项目零 TypeScript 错误

## 客户细分/智能标签系统（01:05-01:20）
- 数据库迁移：086_add_customer_segments.sql — customer_segments（规则JSON + 匹配模式）+ customer_segment_members（分组-客户多对多）
- 后端：
  - 2个 Model：CustomerSegment（initModel 模式）+ CustomerSegmentMember
  - customerSegment.service.js — 核心规则匹配引擎（_buildRuleClause 动态构建 SQL WHERE 子句）：
    - 支持字段：stage/source/assigned_to/tags/last_activity_days/created_days/order_count/total_spent/custom_field
    - match_type：all(AND) / any(OR)
    - refreshMembers：先清空再 SQL 匹配 + bulkCreate 批量插入（200条/批）
    - previewRules：实时预览不落库
    - refreshAllAuto：批量刷新所有 is_auto_refresh=true 的分组
  - segment.controller.js — 9个端点（CRUD + refresh + refreshAll + preview + members）
  - segment.routes.js — /segments 全路由
  - models/index.js — 注册2个模型 + 8组关联
  - routes/index.js — 挂载到 /segments，修复了 marketing.routes 未挂载的 bug
- 前端：
  - API segment.ts — SegmentRule 类型（9字段 × 8运算符）+ 8个API函数
  - SegmentListPage.tsx（~400行）：
    - 分群卡片网格（名称/描述/颜色标签/成员数/规则预览/更新时间）
    - 新建/编辑弹窗（名称/描述/匹配模式/颜色选择/自动刷新开关 + 规则编辑器）
    - 规则编辑器（字段下拉 + 运算符下拉 + 值输入，支持 stage 下拉/tags 输入/custom_field 双输入，实时预览匹配人数 + 示例客户表格）
    - 成员面板（点击卡片 → 侧滑面板展示成员列表）
    - 操作：刷新单个/全部、编辑、删除
  - App.tsx — 路由 /customer-segments
  - sidebarNavConfig.tsx — Sparkles 图标 + 「客户分群」菜单 NEW badge（客户与跟进分组）
- 构建状态：全项目零 TypeScript 错误

## 知识库 / 帮助中心（01:18-01:26）
- 数据库迁移：087_add_knowledge_base.sql — kb_categories + kb_articles（含 view_count/helpful_count/is_ai_generated/seo_keywords）
- 后端：
  - KbCategory + KbArticle Model（initModel）+ knowledgeBase.service.js（分类CRUD + 文章CRUD含计数+搜索+置顶）
  - knowledgeBase.controller.js — 12端点 + knowledgeBase.routes.js — /kb/categories + /kb/articles
  - models/index.js 注册 + routes/index.js 挂载 /kb
- 前端：
  - API knowledgeBase.ts — 12个函数 + KbArticlePage.tsx（~600行：分类树+文章表格+新建/编辑弹窗+搜索分页）
  - App.tsx — 路由 /app/kb-articles，sidebarNavConfig — BookMarked + 「知识库管理」NEW badge
- 构建状态：TypeScript 零错误，Node.js 语法全通过

## AI 教练建议系统（10:20-10:50）
- **数据库**：089_add_coach_suggestions.sql — `ai_coach_suggestions` 表
  - coach_type 6 维度：followup/call/deal/develop/time/overall
  - priority 4 级：1=紧急 2=重要 3=普通 4=建议
  - status 3 态：active/dismissed/implemented
  - context_data JSON 存储生成时的指标快照（today/yesterday/week7/month30/customers/tasks/kpi）
  - impact_score 预留后续自动评估
- **后端 Model**：coachingSuggestion.model.js（initModel 模式）
  - models/index.js 注册 import + initModel + 关联（Tenant/User） + export
- **后端 Service**：coaching.service.js（核心引擎，~400行）
  - `buildEmployeeSnapshot()`：聚合单员工多维度数据快照
    - 从 employeeActivity 获取 today/yesterday/KPI
    - 7天/30天 4表并行查询（CustomerFollowUp/CallRecord/CustomerOrder/Task）
    - 客户池（total/high_intent/new_30d）+ 任务完成率
    - KPI 目标查询（per-user + all fallback）→ 达成百分比
  - `generateDimensionCoaching()`：6 维度独立 AI prompt 工程
    - 每个维度有专属 system prompt + 维度化数据上下文
    - 输出规范：1句评述 + 1个可操作建议 + 1句鼓励，80-120字
    - 内置 invokeAI() 复制 aiContent 同款 DeepSeek/OpenAI 调用逻辑
  - `generateCoaching()`：单员工当日覆盖式生成（先删后建）
  - `generateAllCoaching()`：全租户批量生成（一次获取 activity 共用）
  - `previewCoaching()`：预览不入库
  - `dismissCoaching/implementCoaching`：状态变更
- **后端 Controller**：coaching.controller.js（8端点）
  - list/get/generate/generateAll/preview/dismiss/implement/getCoachTypes
- **后端 Routes**：coaching.routes.js（9条路由）
  - GET /types, GET /, GET /:id, GET /preview/check, POST /generate, POST /generate-all, PATCH /:id/dismiss, PATCH /:id/implement
  - routes/index.js 注册
- **Cron 定时器**：coachingEvaluator.cron.js
  - 每 5 分钟检查，每天 8:00-8:09 触发一次
  - 遍历所有租户 → generateAllCoaching → 防重复（__lastDailyKey 日期标记）
  - 注册到 app.js
- **前端 API**：api/coaching.ts
  - 完整类型：CoachType/Priority/SuggestionStatus/SnapshotData/CoachSuggestion 等 10+ 类型
  - COACH_TYPE_LABELS/ICONS、PRIORITY_LABELS/COLORS、STATUS_LABELS 常量映射
  - 10 个 API 函数（getJson/postJson/patchJson）
- **前端页面**：pages/CoachPage.tsx（~470行）
  - 头部：标题 + 「批量生成全部」按钮
  - 7 维度 Tab 筛选（全部/跟进/通话/成交/开发/时间/综合）
  - 按员工分组的卡片列表：
    - 员工头部：头像+姓名+待处理/已实施统计+「重新生成」按钮
    - 建议卡片：维度图标+优先级标签+状态标签+AI 生成内容+操作按钮（查看指标/已实施/忽略）
    - 指标快照展开面板：今日指标+环比/7天趋势/客户与任务/KPI达成
  - 空状态引导（🧠 图标 + 描述 + 生成按钮）
  - 底部统计信息
- **App + 侧栏**：
  - App.tsx：路由 /app/ai-coach
  - sidebarNavConfig.tsx：Brain 图标 + 「AI 教练建议」菜单（AI 智能分组，NEW badge）+ 新增 Brain import

### 关键架构决策
- **AI 调用自包含**：coaching.service 内置 invokeAI() 复制 aiContent 的同款逻辑（env.deepseekApiKey → deepseek-chat / gpt-4o-mini），不依赖 aiContent 未导出的内部函数
- **当日覆盖式生成**：每次 generateCoaching 先删当天旧建议，避免堆积
- **6 维度独立 prompt**：每个维度有专属 system prompt + 筛选后的数据上下文，而非把所有数据塞给一个 prompt
- **activity 数据复用**：generateAllCoaching 一次获取全租户 activity，逐员工复用，避免 N+1 查询

### 构建验证
- TypeScript 零错误
- 后端 8 文件语法检查全部通过

全项目累计完成 11 个阶段开发

## 部署/测试阶段（11:08-11:18）
### CJS→ESM 批量修复（关键Bug修复）
- **根因**：12 个新创建的 Model 文件使用 CommonJS (`require`/`module.exports`) 语法，与项目的 `"type": "module"` 配置冲突。`node --check` 只检查语法不检查导入，所以之前未发现。
- **修复文件**：
  - 8 个 initModel 模式：kbCategory, kbArticle, customerSegment, customerSegmentMember, notificationRule, notificationRuleLog, browserPushSubscription, coachingSuggestion → 全部改为 `import { DataTypes, Model } from 'sequelize'; export class Xxx extends Model {`
  - 4 个旧工厂模式：marketingCampaign, messageTemplate, marketingMessage, marketingOptout → 完整重写为 ESM `export class` + `initModel()`
  - 1 个缺失 extends：task.model.js 补上 `extends Model`（之前只写了 `export class Task {`）
  - coachingSuggestion.model.js 重写为 initModel 标准模式，coaching.service.js 所有 `.model` 引用改为直接类名
- **后端 Service/Controller/Route CJS 残留**（13个文件）：这些文件使用 `require()` 是 Node.js 的 CJS/ESM 互操作特性，在 `"type": "module"` 项目中仍可正确导入（Node 22 的 cjs-module-lexer 支持），运行时不报错。

### 修复的构建问题
- AnalyticsPage.tsx JSX 语法错误：2处 `Top 10>` 改为 `Top 10`（`>` 在 JSX 文本中需转义）
- AnalyticsPage.tsx 结构错误：`来源分布` div 未闭合，导致 `意向度分布` 和 `热门标签` 错误嵌套。修复为各自独立的 grid 子项
- backend/.env 修复：删除重复的 DASHSCOPE_API_KEY、重复的 PORT、ALIPAY_RETURN_URL 双斜杠

### 最终验证
- ✅ TypeScript 编译：零错误
- ✅ Vite 生产构建：成功（dist/ 输出：index.html 0.62KB + CSS 103KB + JS 1.97MB）
- ✅ 后端测试：17/17 全绿（platformInboxAiAnomaly + aiEmployee.autoSendQualify）
- ✅ 后端语法：所有文件通过
- ⚠️ 已知项：JS bundle 1.97MB 略大（建议后续 code splitting），13个 Service/Route 文件仍用 CJS require（功能正常但风格不一致）

## ECS 部署修复 — wework.syzs.top 后端上线（12:00-12:18）
- **问题链**：API 502 → MySQL root@localhost 认证失败 → 端口冲突 EADDRINUSE null:3000
- **根因分析**：
  1. Nginx HTTPS proxy_pass 指向 3002（不存在），HTTP 指向 3000（ZhiFlow Next.js）
  2. `config/env.js` 无 dotenv 加载（app.js 是 ESM，import env.js 先于任何 dotenv.config 执行），所有 fallback `|| 'root'`/`|| 3000` 生效
  3. PM2 `--update-env` 不传变量时，process.env 全部为空，env.js fallback 链全触发
- **修复**：
  1. Nginx：sed 替换 proxy_pass → 3010（HTTP + HTTPS），添加尾部斜杠
  2. 删除重复进程（wework-api fork 旧进程），统一用 syqw-api cluster
  3. 最终启动命令：`PORT=3010 DB_USER=wework_app DB_PASSWORD=MySecretPass123 DB_HOST=127.0.0.1 DB_NAME=wework_saas pm2 start backend/src/app.js --name syqw-api -i 2 --max-memory-restart 400M --update-env`
- **验证**：
  - `curl http://127.0.0.1:3010/health` → `{"ok":true,"service":"wework-saas-backend"}`
  - `curl http://127.0.0.1/api/health`（Nginx）→ 200 OK
  - PM2 日志：`SELECT 1+1 AS result`（MySQL 连接成功），无 Access Denied
  - PM2 save + systemd 自启已配置
- **教训**：ESM 项目中 env.js 作为 static import 会在任何代码执行前完成，dotenv 无法在 import 之前注入环境变量。生产环境应通过 PM2 的 `--update-env` 或 ecosystem.config.cjs 的 `env` 字段传递变量

## 智能通知与提醒系统（01:30-02:00）
- **数据库**：088_add_notification_rules.sql — 3张表
  - `notification_rules`：自定义通知规则（trigger_type: schedule/event/cron，trigger_config JSON，channels JSON: in_app/wecom/browser，recipient_type: specific/role/owner/all，template JSON支持{{变量}}，priority 4级，cooldown_minutes，max_per_run）
  - `notification_rule_logs`：执行日志（recipients_count/channels_used/status/error_message）
  - `browser_push_subscriptions`：浏览器 Push 订阅（endpoint/p256dh/auth + user_agent/device_name，UNIQUE(user_id, endpoint)）
- **后端 Model 层**（3个文件）：
  - NotificationRule / NotificationRuleLog / BrowserPushSubscription（initModel + association 模式）
  - models/index.js 注册 3 个模型 + 关联关系
- **后端 Service 层**（核心 3 个文件）：
  - `notificationRule.service.js`：规则 CRUD + 事件评估引擎（_evaluateEvent 支持 7 种事件类型）
    - followup_overdue：查询最后跟进超 N 天的客户 → 通知 owner
    - stage_changed / deal_won / customer_created：外部传入 customer_id 匹配
    - task_due：查询 tasks 表即将到期的任务 → 通知 assignee
    - intent_high：CustomerScore 表中 score >= threshold 的高意向客户
    - customer_inactive：updated_at < N天前 的沉寂客户
    - 接收人解析：specific(user_ids) / role(role_id→查User) / owner(动态) / all(全员)
    - 模板渲染：renderTemplate 替换 {{variable}}
  - `notificationDispatcher.service.js`：多渠道路由分发器
    - dispatch(rule)：检查冷却→评估条件→获取User+Tenant→逐用户多渠道发送
    - in_app：调用 notification.service.createNotification 写入 notifications 表（❗首次真正打通通知链路）
    - wecom：动态 import sendAgentTextMessage，传入 tenant 对象 + user.wework_userid
    - browser：调用 browserPushService.sendToUser
    - triggerEvent(event, context)：外部事件批量触发匹配规则
  - `browserPush.service.js`：VAPID Web Push 完整实现
    - 自动生成/配置 VAPID 密钥对
    - subscribe：findOrCreate 保存订阅 → 更新已有
    - unsubscribe：标记 is_active=false
    - sendToUser：逐个 endpoint 发送 + 自动清理 410/404 失效订阅
    - 依赖：npm install web-push（新增）
- **后端 Controller + Routes**（4个文件）：
  - `notificationRule.controller.js`：8端点（list/get/create/update/delete/toggle/trigger + eventTypes 常量）
  - `browserPush.controller.js`：4端点（vapid-public-key/subscribe/unsubscribe/status）
  - `notificationRule.routes.js`：/notification-rules/*（9条路由）
  - `browserPush.routes.js`：/browser-push/*（4条路由）
  - routes/index.js 注册 2 个路由模块
- **Cron 定时器**：`notificationRuleEvaluator.cron.js`
  - 每 2 分钟评估所有 enabled + trigger_type IN (schedule, cron) 的规则
  - 注册到 app.js（registerNotificationRuleEvaluatorCron）
- **前端浏览器 Push 基础设施**：
  - `public/sw.js`：Service Worker 监听 push 事件 → showNotification，notificationclick 跳转对应页面
  - `api/browserPush.ts`：VapidPublicKey / PushSubscriptionStatus 类型 + 5 个 API 函数 + urlBase64ToUint8Array 工具
  - `hooks/usePushSubscription.ts`：React Hook 封装 SW 注册 + VAPID 订阅 + 权限管理全流程
- **前端通知规则管理**：
  - `api/notificationRules.ts`：完整类型定义（TriggerType/RecipientType/RulePriority/TriggerConfig/RecipientConfig/RuleTemplate/NotificationRule/RuleLog/EventType）+ 8 个 API 函数
  - `pages/NotificationRulesPage.tsx`（~500行）：
    - 规则卡片网格：状态指示灯+触发类型+优先级标签+触发条件摘要+渠道图标+模板预览+操作按钮（启用/禁用/手动触发/日志/编辑/删除）
    - 新建/编辑弹窗（~300行内联样式）：
      - 基本信息（名称/描述）+ 触发类型切换（定时/事件/Cron）
      - 定时：类型下拉（每天/每周/间隔）+ 时间选择器 + 间隔分钟数
      - 事件：7种事件类型下拉 + 动态筛选条件（逾期天数/意向分阈值/不活跃天数/提前小时）
      - Cron：表达式输入 + 格式提示
      - 渠道多选按钮（站内/企微/浏览器，选中高亮）
      - 模板编辑（标题/正文/链接 3 输入 + 变量提示标签，点击变量自动插入）
      - 高级设置折叠面板（冷却时间/单次上限/优先级）
    - 日志面板（时间/规则/接收人数/渠道标签/状态标签 success/partial/failed）
    - 浏览器推送订阅按钮（集成 usePushSubscription Hook）
    - 空状态提示
  - App.tsx — 路由 /app/notification-rules
  - sidebarNavConfig.tsx — BellDot 图标 + 「智能通知规则」菜单（工作台分组，NEW badge）

### 关键架构决策
- **通知链路首次打通**：之前 notifications 表只有读取没有写入，本次通过 notificationDispatcher 的 in_app 渠道首次将实际通知写入站内通知中心，NotificationBell 的 30s 轮询将能正确显示未读角标
- **企微通知复用现有基础设施**：通过动态 import sendAgentTextMessage 复用已验证的企微消息通路（13+服务在使用）
- **VAPID 密钥设计**：优先读环境变量，否则自动生成（内存中，重启重置），避免配置缺失时服务不可用
- **事件规则 JSON 查询**：使用 MySQL JSON_EXTRACT 查找匹配事件的规则，突破 Sequelize JSON 字段查询限制

### 构建验证
- TypeScript 编译：零错误
- 后端 JS 语法检查：Models/Services/Controllers/Routes/Cron 共 12 个文件全部通过
- 全项目累计完成 10 个阶段开发

## ECS Guest-Login 最终修复（14:56）
- **问题**：wework.syzs.top guest-login 返回 401/502
- **根因**：PM2 进程因 DB 列缺失 (`demo_mode`, `perm_codes`) 不断崩溃重启，导致端口 3010 未监听
- **修复**：
  1. 删除错误 demo 用户 (id=9999)，插入 role(1, 管理员, tenant_id=9999) 和正确 demo 用户 (id=9998, tenant_id=9999, role='admin', role_id=1, demo_mode=1)
  2. 确认列均存在：`users.demo_mode` (tinyint(1)), `roles.perm_codes` (json), `roles.permissions` (json)
  3. PM2 重启后端口 3010 正常监听
- **验证**：`POST /api/v1/auth/guest-login` → `{"code":0,"message":"登录成功"}` JWT含14项权限+admin角色 ✅
- **最终部署**：syqw-api 端口 3010 (PM2 cluster 2实例)，Nginx proxy_pass → 127.0.0.1:3010
