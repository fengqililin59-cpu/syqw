# 后端 API 根（不含末尾斜杠）：
# - 本地 `npm run dev`：写在 .env.development（已提供示例），勿在根 .env 写 localhost（避免与 .env.production 合并顺序踩坑）。
# - 生产 `npm run build`：仅读 .env.production，须留空以走同域 /api。
# Docker 同域反代：与生产一致，VITE_API_URL 留空。
#
# 可选：页脚备案与公司主体（默认已内置杭州中数云科智慧科技有限公司 / 浙ICP备2026009605号-1）
# VITE_LEGAL_COMPANY_NAME=杭州中数云科智慧科技有限公司
# VITE_LEGAL_ICP_NUMBER=浙ICP备2026009605号-1
