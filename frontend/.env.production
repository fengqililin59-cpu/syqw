# 生产构建（npm run build）使用本文件，会覆盖 .env 里的 VITE_API_URL。
# 与 Nginx「同域反代 /api → Node」一致时必须留空，浏览器才会请求 https://wework.syzs.top/api/v1 ，否则会误连 localhost 导致 CORS。
VITE_API_URL=
# 0=隐藏支付宝按钮；1=显示（改后须 npm run build 并 rsync 到 /var/www/wework）
VITE_ALIPAY_ENABLED=1
